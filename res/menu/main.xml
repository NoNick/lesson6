<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/refresh_feed"
          android:icon="@android:drawable/ic_menu_rotate"
          android:showAsAction="ifRoom" android:title="refresh"/>
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/add_feed"
          android:icon="@android:drawable/ic_menu_add"
          android:showAsAction="ifRoom" android:title="add"/>
    <item android:id="@+id/del_feed"
          android:icon="@android:drawable/ic_menu_delete"
          android:showAsAction="ifRoom" android:title="remove"/>
</menu>